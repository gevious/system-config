##
## Basics
## 
bindsym $mod+$modMeta+r reload
bindsym $mod+q kill
#bindsym $mod+r restart
bindsym $mod+$modMeta+e exec swaynag -t warning -m 'Really exit sway?' -B 'Yes, exit sway' 'swaymsg exit'
bindsym --release $mod+$modMeta+x exec xkill

##
## Moving around:
##
# Toggle focus between floating and tiling windows
# Move your focus around
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# Move the focused window with the same, but add Shift
bindsym $mod+$modMeta+$left move left
bindsym $mod+$modMeta+$down move down
bindsym $mod+$modMeta+$up move up
bindsym $mod+$modMeta+$right move right

# Scratch pad
bindsym $mod+$modMeta+minus move scratchpad
bindsym $mod+minus scratchpad show

# Swap focus between the tiling area and the floating area
bindsym $mod+$modMeta+space focus mode_toggle

##
## Workspace switching
##
# Window layout styles
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
#bindsym $mod+e layout toggle split
bindsym $mod+f fullscreen
bindsym $mod+o floating toggle
bindsym $mod+p focus mode_toggle

# Switch workspaces
bindsym $mod+0 workspace $ws0
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9

# move focused container to workspace
bindsym $mod+$modMeta+0 move container to workspace $ws0; workspace $ws0
bindsym $mod+$modMeta+1 move container to workspace $ws1; workspace $ws1
bindsym $mod+$modMeta+2 move container to workspace $ws2; workspace $ws2
bindsym $mod+$modMeta+3 move container to workspace $ws3; workspace $ws3
bindsym $mod+$modMeta+4 move container to workspace $ws4; workspace $ws4
bindsym $mod+$modMeta+5 move container to workspace $ws5; workspace $ws5
bindsym $mod+$modMeta+6 move container to workspace $ws6; workspace $ws6
bindsym $mod+$modMeta+7 move container to workspace $ws7; workspace $ws7
bindsym $mod+$modMeta+8 move container to workspace $ws8; workspace $ws8
bindsym $mod+$modMeta+9 move container to workspace $ws9; workspace $ws9

##
## Resizing containers:
##
bindsym $mod+r mode "resize"
# resize window (you can also use the mouse for that):
mode "resize" {
    bindsym $up resize grow width 2 px or 2 ppt
    bindsym $down resize shrink width 2 px or 2 ppt
    bindsym $mod+$up resize grow height 2 px or 2 ppt
    bindsym $mod+$down resize shrink height 2 px or 2 ppt

# back to normal: Enter or Escape
	bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Use Mouse+$mod to drag floating windows to their wanted position
#floating_modifier $mod

# App launcher
set $term "ghostty"
set $launcher "fuzzel"

bindsym $mod+space exec $launcher
bindsym $mod+$modMeta+Return exec $term & swaymsg '[app_id=$term]' focus 

# shutdown computer
bindsym $mod+$modMeta+BackSpace exec shutdown 0

#  ----------
#  Multimedia
#  ----------
# Volume control
set $sink `pactl list short sinks | grep RUNNING | cut -f1`
bindsym --locked XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume $sink +5%
bindsym --locked XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume $sink -5%
bindsym --locked XF86AudioMute exec --no-startup-id pactl set-sink-mute $sink toggle && notify-send "$(pactl get-sink-mute $sink)"

# audio control
#bindsym XF86AudioPlay exec playerctl play
#bindsym XF86AudioNext exec playerctl next
#bindsym XF86AudioPrev exec playerctl previous
#bindsym XF86AudioPause exec playerctl pause

# brighness
bindsym --locked XF86MonBrightnessDown exec brightnessctl set 10%-
bindsym --locked XF86MonBrightnessUp exec brightnessctl set 10%+

## Redirect sound to headphones
#bindsym $mod+p exec /usr/local/bin/switch-audio-port

# Set sound output to headphones
#bindsym $mod+Shift+Ctrl+h exec pactl set-default-sink bluez_output.F8_AB_E5_56_A1_E9.1 && notify-send "Sound in jbl" 
#bindsym $mod+Shift+Ctrl+h exec pactl set-default-sink bluez_output.80_C3_BA_32_48_1E.1 && notify-send "Sound in Sennheiser"

# Set sound output to laptop
#bindsym $mod+Shift+Ctrl+l exec pactl set-default-sink alsa_output.pci-0000_00_1f.3.analog-stereo && notify-send "Sound on Laptop"


#-------------------
# Script keybindings
#-------------------

# lock by blurring the screen:
bindsym $mod+Backspace exec ~/.config/sway/scripts/blur-lock

# screenshot of monitor in focus
bindsym $mod+$modMeta+equal exec ~/.config/sway/scripts/screenshot
